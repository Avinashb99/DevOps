git init
ls -a

git config --global user.name "Polarapu Prasad"
git config --global user.name "devopstraining2015@gmail.com"

git config --list

touch file1
ls
git status
git add file1
git status
git commit -m "test commit" file1
git status
git log
git show cid

touch file2 file3 file4
ls
git status
git add file2 file3 file4
or
git add .
or
git add *
or
git add -A
git status
git commit -m "label"
git log

git reset HEAD file-name  (staging to workspace)
git reset --soft cid  (local to staging)
git reset --mixed cid  (local to workspace)
git commit -m "label" file-name (workspace to local) *only modified files

git clone https://github.com/Avinashb99/DevOps.git
ls
cd DevOps
touch avi
git status
git add avi
git commit -m "sample commit"
git push

-----------------
rm -rf file-name  (delete a file/dir)
git status
git add .
git commit -m "deleted file"
git push